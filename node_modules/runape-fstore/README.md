
```javascript
var fstore = require('runape-fstore');
fstore.setFileName("./Scripts/myStorage.json")   //Optionally set where the object is saved. ./fstore.json is default.
fstore.set({"Weather":"Sunny"});                 //Store the object to the file
console.log(fstore.get().Weather);               //Get and print the Weather
```